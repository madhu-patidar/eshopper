$("#item-<%=@cart_item[:id]%>").remove();

$('#total_area').html("<%= j (render partial: 'total_area', :locals => {:tax => @tax, :cart_sub_total => @cart_sub_total, :discount => @discount}) %>");

$('#cart-item-count').html(<% if @cart_items.count > 0 %>" (" +<%= @cart_items.count %>+ " )" <% else %> "" <% end %>);

$('#empty').html("<%= j (render partial: 'empty_item') %>");

$('#table-head').html("<%= j (render partial: 'table_head') %>");


