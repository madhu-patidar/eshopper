<%if @products.present? %>
  <% @products.each do |product| %>
    <div class="tab-pane fade active in" id="product-list-<%=product.id%>" >
      <%= render "components/content" ,product: product %>
    </div>
  <%end%>
<% else %>
  <h2 align="center"> Product not available</h2>
<% end %>
